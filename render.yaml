services:
  - type: web
    name: chatkit-agent
    env: python
    buildCommand: ""
    startCommand: "uvicorn server:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
